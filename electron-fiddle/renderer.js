// Intentionally empty.
//
// This Fiddle loads an external URL via win.loadURL() in main.js, so the local
// renderer.js / index.html are never executed. Electron Fiddle expects them to
// exist, so they're kept as stubs.
